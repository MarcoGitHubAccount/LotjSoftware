<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
	<AliasPackage>
		<Alias isActive="yes" isFolder="no">
			<name>get encryption for channel</name>
			<script>local chan = matches[2]
math.randomseed(tonumber(chan))
local rot = tonumber(matches[3])
if rot &gt; 0 then
  for i=1,rot do
    math.random()
  end
end
local enc = string.format("%03d",math.random()*1000)
cecho("\n&lt;white&gt;"..chan.." "..enc.."\n")</script>
			<command></command>
			<packageName></packageName>
			<regex>^genenc (.*),(.*)$</regex>
		</Alias>
	</AliasPackage>
</MudletPackage>
